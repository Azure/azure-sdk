#### Configuration for JavaScript Libraries

~ Draft

~~ Must
Use the `@azure/core-configuration` package.  The `@azure/core-configuration` package implements the [general guidelines for configuration]:

1. Retrieve any relevant settings from the environment
1. Retrieve any relevant global settings from the consumers code
~~
~