entries:
- Name: typespec
  Version: 0.9.0
  DisplayName: TypeSpec Core
  ServiceName: TypeSpec
  VersionType: Beta
  Hidden: false
  ChangelogUrl: https://github.com/Azure/azure-sdk-for-rust/tree/typespec@0.9.0/sdk/typespec/CHANGELOG.md
  ChangelogContent: |-
    #### Breaking Changes

    - Changed `RawResponse::body()` to return a `&ResponseBody` instead of `&Bytes`. `ResponseBody` wraps `&Bytes`, and implements `AsRef<[u8]>` and `Deref<Target = [u8]>`.
    - Changed `RawResponse::into_body()` to return a `ResponseBody` instead of `Bytes`. `ResponseBody` wraps `&Bytes`, and implements `AsRef<[u8]>` and `Deref<Target = [u8]>`.
    - Changed `RawResponse::json()` from `async` to a sync function. The body was already buffered.
    - Changed `RawResponse::xml()` from `async` to a sync function. The body was already buffered.
    - Moved `AsHeaders`, `FromHeaders`, `Header`, `Headers`, `HeaderName`, and `HeaderValue` to `http::headers` module to align with `typespec_client_core`.
    - Removed `ErrorKind::http_response()`. Construct an `ErrorResponse::HttpResponse` variant instead.
    - Renamed a number of construction functions for `Error` to align with [guidelines](https://azure.github.io/azure-sdk/rust_introduction.html)"
    - Renamed `Error::full()` to `Error::with_error()`.
    - Renamed `Error::with_message()` to `Error::with_message_fn()`.
    - Renamed `Error::message()` to `Error::with_message()`.
    - Renamed `Error::with_context()` to `Error::with_context_fn()`.
    - Renamed `Error::context()` to `Error::with_context()`.
    - Renamed `ResultExt::map_kind()` to `ResultExt::with_kind()`.
    - Renamed `ResultExt::with_context()` to `ResultExt::with_context_fn()`.
    - Renamed `ResultExt::context()` to `ResultExt::with_context()`.

    #### Features Added

    - Added `Error::with_error_fn()`.
    - Added `http::response::ResponseBody`.
    - Added `RawResponse::deconstruct()`.
    - Added `ResponseBody::from_bytes()`.
- Name: typespec_macros
  Version: 0.8.0
  DisplayName: TypeSpec Core Macros
  ServiceName: TypeSpec
  VersionType: Beta
  Hidden: false
  ChangelogUrl: https://github.com/Azure/azure-sdk-for-rust/tree/typespec_macros@0.8.0/sdk/typespec/typespec_macros/CHANGELOG.md
  ChangelogContent: |-
    #### Breaking Changes

    - Renamed a number of construction functions for `Error` to align with [guidelines](https://azure.github.io/azure-sdk/rust_introduction.html)"
    - Renamed `Error::full()` to `Error::with_error()`.
    - Renamed `Error::with_message()` to `Error::with_message_fn()`.
    - Renamed `Error::message()` to `Error::with_message()`.
    - Renamed `Error::with_context()` to `Error::with_context_fn()`.
    - Renamed `Error::context()` to `Error::with_context()`.
    - Renamed `ResultExt::map_kind()` to `ResultExt::with_kind()`.
    - Renamed `ResultExt::with_context()` to `ResultExt::with_context_fn()`.
    - Renamed `ResultExt::context()` to `ResultExt::with_context()`.

    #### Features Added

    - Added `Error::with_error_fn()`.
- Name: typespec_client_core
  Version: 0.8.0
  DisplayName: TypeSpec Client Core
  ServiceName: TypeSpec
  VersionType: Beta
  Hidden: false
  ChangelogUrl: https://github.com/Azure/azure-sdk-for-rust/tree/typespec_client_core@0.8.0/sdk/typespec/typespec_client_core/CHANGELOG.md
  ChangelogContent: |-
    #### Breaking Changes

    - Changed `ClientOptions::retry` from `Option<RetryOptions>` to `RetryOptions`.
    - Changed `DeserializeWith::deserialize_with()` to be sync.
    - Changed `Pipeline::send()` to return a `Result<RawResponse>`.
    - Changed `RawResponse::body()` to return a `&ResponseBody` instead of `&Bytes`. `ResponseBody` wraps `&Bytes`, and implements `AsRef<[u8]>` and `Deref<Target = [u8]>`.
    - Changed `RawResponse::into_body()` to return a `ResponseBody` instead of `Bytes`. `ResponseBody` wraps `&Bytes`, and implements `AsRef<[u8]>` and `Deref<Target = [u8]>`.
    - Changed `RawResponse::json()` from `async` to a sync function. The body was already buffered.
    - Changed `RawResponse::xml()` from `async` to a sync function. The body was already buffered.
    - Changed `Response<T, F>` to fully sync; it holds a `RawResponse` that was already buffered entirely from the service so no longer needs or defines async functions.
    - Changed `ResponseBody::json()` and `xml()` to borrow `self`.
    - Removed `create_extensible_enum` and `create_enum` macros.
    - Removed `BufResponse::json()`.
    - Removed `BufResponse::xml()`.
    - Removed `CustomHeadersPolicy` from public API.
    - Removed `ErrorKind::http_response()`. Construct an `ErrorResponse::HttpResponse` variant instead.
    - Removed `ExponentialRetryPolicy` from public API.
    - Removed `FixedRetryPolicy` from public API.
    - Removed `LoggingPolicy` from public API.
    - Removed `NoRetryPolicy` from public API.
    - Removed implementation of `Stream` for `ResponseBody`.
    - Renamed `TransportOptions::new_custom_policy()` to `Transport::with_policy()`.
    - Renamed `TransportOptions` to `Transport`.
    - Renamed a number of construction functions for `Error` to align with [guidelines](https://azure.github.io/azure-sdk/rust_introduction.html)"
    - Renamed `Error::full()` to `Error::with_error()`.
    - Renamed `Error::with_message()` to `Error::with_message_fn()`.
    - Renamed `Error::message()` to `Error::with_message()`.
    - Renamed `Error::with_context()` to `Error::with_context_fn()`.
    - Renamed `Error::context()` to `Error::with_context()`.
    - Renamed `ResultExt::map_kind()` to `ResultExt::with_kind()`.
    - Renamed `ResultExt::with_context()` to `ResultExt::with_context_fn()`.
    - Renamed `ResultExt::context()` to `ResultExt::with_context()`.
    - Replaced implementation of `From<BufResponse>` for `Response<T, F>` to `From<RawResponse>`.
    - Replaced implementation of `From<Response<T, F>>` for `BufResponse` to `From<AsyncResponse<T>>`.

    #### Features Added

    - Added `AsyncResponse<T>` for responses that may stream the body outside the HTTP pipeline. This replaces `Response<T, F>` requiring an async read of the body that occurred outside the HTTP pipeline.
    - Added `Error::with_error_fn()`.
    - Added `http::response::BufResponseBody`, which also implements `Stream`.
    - Added `RawResponse::deconstruct()`.
    - Added `ResponseBody::into_string()`.
    - Added `ResponseBody::from_bytes()`.
    - Added a `Pipeline::stream()` to return a `Result<BufResponse>`.
    - Implemented `AsRef<[u8]>` and `Deref<Target = [u8]>` for `ResponseBody`.
- Name: azure_core_amqp
  Version: 0.8.0
  DisplayName: Core - AMQP
  ServiceName: core
  VersionType: Beta
  Hidden: false
  ChangelogUrl: https://github.com/Azure/azure-sdk-for-rust/tree/azure_core_amqp@0.8.0/sdk/core/azure_core_amqp/CHANGELOG.md
  ChangelogContent: |-
    #### Breaking Changes

    - Removed non-idiomatic accessor functions from `AmqpDescribed`, `AmqpSessionOptions`, `AmqpDescribedError`, `AmqpMessage`, and `AmqpTarget`
    - Renamed the "cplusplus" feature to "ffi" because "ffi" is more idiomatic to Rust.
- Name: azure_core_opentelemetry
  Version: 0.3.0
  DisplayName: Core - OpenTelemetry
  ServiceName: core
  VersionType: Beta
  Hidden: false
  ChangelogUrl: https://github.com/Azure/azure-sdk-for-rust/tree/azure_core_opentelemetry@0.3.0/sdk/core/azure_core_opentelemetry/CHANGELOG.md
  ChangelogContent: |-
    #### Other Changes

    - Updated dependencies.
- Name: azure_core_macros
  Version: 0.3.0
  DisplayName: Core - Macros
  ServiceName: core
  VersionType: Beta
  Hidden: false
  ChangelogUrl: https://github.com/Azure/azure-sdk-for-rust/tree/azure_core_macros@0.3.0/sdk/core/azure_core_macros/CHANGELOG.md
  ChangelogContent: |-
    #### Other Changes

    - Updated dependencies.
- Name: azure_core
  Version: 0.29.0
  DisplayName: Core
  ServiceName: core
  VersionType: Beta
  Hidden: false
  ChangelogUrl: https://github.com/Azure/azure-sdk-for-rust/tree/azure_core@0.29.0/sdk/core/azure_core/CHANGELOG.md
  ChangelogContent: |-
    #### Breaking Changes


    - Changed `ClientOptions::retry` from `Option<RetryOptions>` to `RetryOptions`.
    - Changed `DeserializeWith::deserialize_with()` to be sync.
    - Changed `Pipeline::send()` to return a `Result<RawResponse>`.
    - Changed `RawResponse::body()` to return a `&ResponseBody` instead of `&Bytes`. `ResponseBody` wraps `&Bytes`, and implements `AsRef<[u8]>` and `Deref<Target = [u8]>`.
    - Changed `RawResponse::into_body()` to return a `ResponseBody` instead of `Bytes`. `ResponseBody` wraps `&Bytes`, and implements `AsRef<[u8]>` and `Deref<Target = [u8]>`.
    - Changed `RawResponse::json()` from `async` to a sync function. The body was already buffered.
    - Changed `RawResponse::xml()` from `async` to a sync function. The body was already buffered.
    - Changed `Response<T, F>` to fully sync; it holds a `RawResponse` that was already buffered entirely from the service so no longer needs or defines async functions.
    - Changed `ResponseBody::json()` and `xml()` to borrow `self`.
    - Removed `create_extensible_enum` and `create_enum` macros.
    - Removed `BufResponse::json()`.
    - Removed `BufResponse::xml()`.
    - Removed `CustomHeadersPolicy` from public API.
    - Removed `ErrorKind::http_response()`. Construct an `ErrorResponse::HttpResponse` variant instead.
    - Removed `ExponentialRetryPolicy` from public API.
    - Removed `FixedRetryPolicy` from public API.
    - Removed `LoggingPolicy` from public API.
    - Removed `NoRetryPolicy` from public API.
    - Removed implementation of `Stream` for `ResponseBody`.
    - Removed several unreferenced HTTP headers and accessor structures for those headers.
    - Renamed `TransportOptions::new_custom_policy()` to `Transport::with_policy()`.
    - Renamed `TransportOptions` to `Transport`.
    - Renamed a number of construction functions for `Error` to align with [guidelines](https://azure.github.io/azure-sdk/rust_introduction.html)"
    - Renamed `Error::full()` to `Error::with_error()`.
    - Renamed `Error::with_message()` to `Error::with_message_fn()`.
    - Renamed `Error::message()` to `Error::with_message()`.
    - Renamed `Error::with_context()` to `Error::with_context_fn()`.
    - Renamed `Error::context()` to `Error::with_context()`.
    - Renamed `ResultExt::map_kind()` to `ResultExt::with_kind()`.
    - Renamed `ResultExt::with_context()` to `ResultExt::with_context_fn()`.
    - Renamed `ResultExt::context()` to `ResultExt::with_context()`.
    - Replaced implementation of `From<BufResponse>` for `Response<T, F>` to `From<RawResponse>`.
    - Replaced implementation of `From<Response<T, F>>` for `BufResponse` to `From<AsyncResponse<T>>`.

    #### Bugs Fixed

    - `ErrorKind::HttpResponse { raw_response, .. }` may have been incorrectly `None`.

    #### Features Added

    - Added `Error::with_error_fn()`.
    - Added `AsyncResponse<T>` for responses that may stream the body outside the HTTP pipeline. This replaces `Response<T, F>` requiring an async read of the body that occurred outside the HTTP pipeline.
    - Added `http::response::BufResponseBody`, which also implements `Stream`.
    - Added a `Pipeline::stream()` to return a `Result<BufResponse>`.
    - Added `RawResponse::deconstruct()`.
    - Added `ResponseBody::into_string()`.
    - Added `ResponseBody::from_bytes()`.
    - Added the `cloud` module with types for configuring clients to use different Azure clouds.
    - Implemented `AsRef<[u8]>` and `Deref<Target = [u8]>` for `ResponseBody`.
- Name: typespec
  Version: 0.9.1
  DisplayName: TypeSpec Core
  ServiceName: TypeSpec
  VersionType: Beta
  Hidden: false
  ChangelogUrl: https://github.com/Azure/azure-sdk-for-rust/tree/typespec@0.9.1/sdk/typespec/CHANGELOG.md
  ChangelogContent: |-
    #### Bugs Fixed

    - Fix feature documentation ([#3118](https://github.com/Azure/azure-sdk-for-rust/issues/3118))
- Name: azure_storage_blob
  Version: 0.6.0
  DisplayName: Storage - Blobs
  ServiceName: Storage
  VersionType: Beta
  Hidden: false
  ChangelogUrl: https://github.com/Azure/azure-sdk-for-rust/tree/azure_storage_blob@0.6.0/sdk/storage/azure_storage_blob/CHANGELOG.md
  ChangelogContent: |-
    #### Breaking Changes

    - Changed `BlobClient::download()` to return an `AsyncResponse` instead of a `Response`. This allows for streaming large payloads outside the internal HTTP pipeline.
    - Client methods that return a `Response<T>>` asynchronously buffer the entire model within the internal pipeline, so `into_body()` and other methods on the response are no longer async.

    #### Features Added

    - Added support for `exists` to `BlobClient` and `BlobContainerClient`.
- Name: typespec_client_core
  Version: 0.8.1
  DisplayName: TypeSpec Client Core
  ServiceName: TypeSpec
  VersionType: Beta
  Hidden: false
  ChangelogUrl: https://github.com/Azure/azure-sdk-for-rust/tree/typespec_client_core@0.8.1/sdk/typespec/typespec_client_core/CHANGELOG.md
  ChangelogContent: |-
    #### Bugs Fixed

    - Fix feature documentation ([#3118](https://github.com/Azure/azure-sdk-for-rust/issues/3118))
- Name: azure_core_amqp
  Version: 0.8.1
  DisplayName: Core - AMQP
  ServiceName: core
  VersionType: Beta
  Hidden: false
  ChangelogUrl: https://github.com/Azure/azure-sdk-for-rust/tree/azure_core_amqp@0.8.1/sdk/core/azure_core_amqp/CHANGELOG.md
  ChangelogContent: |-
    #### Bugs Fixed

    - Fix feature documentation ([#3118](https://github.com/Azure/azure-sdk-for-rust/issues/3118))
- Name: azure_core_macros
  Version: 0.3.1
  DisplayName: Core - Macros
  ServiceName: core
  VersionType: Beta
  Hidden: false
  ChangelogUrl: https://github.com/Azure/azure-sdk-for-rust/tree/azure_core_macros@0.3.1/sdk/core/azure_core_macros/CHANGELOG.md
  ChangelogContent: |-
    #### Bugs Fixed

    - Fix feature documentation ([#3118](https://github.com/Azure/azure-sdk-for-rust/issues/3118))
- Name: azure_core
  Version: 0.29.1
  DisplayName: Core
  ServiceName: core
  VersionType: Beta
  Hidden: false
  ChangelogUrl: https://github.com/Azure/azure-sdk-for-rust/tree/azure_core@0.29.1/sdk/core/azure_core/CHANGELOG.md
  ChangelogContent: |-
    #### Bugs Fixed

    - Fix feature documentation ([#3118](https://github.com/Azure/azure-sdk-for-rust/issues/3118))

    #### Breaking Changes

    - Removed the `azurite_workaround` feature (unused).
- Name: typespec_macros
  Version: 0.8.1
  DisplayName: TypeSpec Core Macros
  ServiceName: TypeSpec
  VersionType: Beta
  Hidden: false
  ChangelogUrl: https://github.com/Azure/azure-sdk-for-rust/tree/typespec_macros@0.8.1/sdk/typespec/typespec_macros/CHANGELOG.md
  ChangelogContent: |-
    #### Bugs Fixed

    - Fix feature documentation ([#3118](https://github.com/Azure/azure-sdk-for-rust/issues/3118))
- Name: azure_core_opentelemetry
  Version: 0.3.1
  DisplayName: Core - OpenTelemetry
  ServiceName: core
  VersionType: Beta
  Hidden: false
  ChangelogUrl: https://github.com/Azure/azure-sdk-for-rust/tree/azure_core_opentelemetry@0.3.1/sdk/core/azure_core_opentelemetry/CHANGELOG.md
  ChangelogContent: |-
    #### Bugs Fixed

    - Fix feature documentation ([#3118](https://github.com/Azure/azure-sdk-for-rust/issues/3118))
- Name: azure_data_cosmos
  Version: 0.28.0
  DisplayName: Cosmos DB
  ServiceName: Cosmos DB
  VersionType: Beta
  Hidden: false
  ChangelogUrl: https://github.com/Azure/azure-sdk-for-rust/tree/azure_data_cosmos@0.28.0/sdk/cosmos/azure_data_cosmos/CHANGELOG.md
  ChangelogContent: |-
    #### Features Added

    - Added `Query::with_text()` and `Query::append_text()` methods to modify query text after creation ([#3044](https://github.com/Azure/azure-sdk-for-rust/pull/3044))
    - Added `PatchDocument::with_condition()` methods to allow setting a condition on a patch operation ([#2969](https://github.com/Azure/azure-sdk-for-rust/pull/2969))

    #### Breaking Changes

    - Client methods that return a `Response<T>>` asynchronously buffer the entire model within the internal pipeline, so `into_body()` and other methods on the response are no longer async.
- Name: azure_security_keyvault_certificates
  Version: 0.7.0
  DisplayName: Key Vault - Certificates
  ServiceName: Key Vault
  VersionType: Beta
  Hidden: false
  ChangelogUrl: https://github.com/Azure/azure-sdk-for-rust/tree/azure_security_keyvault_certificates@0.7.0/sdk/keyvault/azure_security_keyvault_certificates/CHANGELOG.md
  ChangelogContent: |-
    #### Breaking Changes

    - Client methods that return a `Response<T>>` asynchronously buffer the entire model within the internal pipeline, so `into_body()` and other methods on the response are no longer async.
    - Renamed `KeyType::EC` to `KeyType::Ec` to align with guidelines.
    - Renamed `KeyType::RSA` to `KeyType::Rsa` to align with guidelines.
- Name: azure_security_keyvault_keys
  Version: 0.8.0
  DisplayName: Key Vault - Keys
  ServiceName: Key Vault
  VersionType: Beta
  Hidden: false
  ChangelogUrl: https://github.com/Azure/azure-sdk-for-rust/tree/azure_security_keyvault_keys@0.8.0/sdk/keyvault/azure_security_keyvault_keys/CHANGELOG.md
  ChangelogContent: |-
    #### Breaking Changes

    - Client methods that return a `Response<T>>` asynchronously buffer the entire model within the internal pipeline, so `into_body()` and other methods on the response are no longer async.
    - Renamed `KeyType::EC` to `KeyType::Ec` to align with guidelines.
    - Renamed `KeyType::RSA` to `KeyType::Rsa` to align with guidelines.
    - Renamed `EncryptionAlgorithm::RSA1_5` to `EncryptionAlgorithm::Rsa1_5` to align with guidelines.
    - Renamed `EncryptionAlgorithm::RsaOAEP256` to `EncryptionAlgorithm::RsaOaep256` to align with guidelines.
    - Renamed several variants in `SignatureAlgorithm` to align with guidelines e.g., `ES256K` to `Es256K`.
- Name: azure_security_keyvault_secrets
  Version: 0.8.0
  DisplayName: Key Vault - Secrets
  ServiceName: Key Vault
  VersionType: Beta
  Hidden: false
  ChangelogUrl: https://github.com/Azure/azure-sdk-for-rust/tree/azure_security_keyvault_secrets@0.8.0/sdk/keyvault/azure_security_keyvault_secrets/CHANGELOG.md
  ChangelogContent: |-
    #### Breaking Changes

    - Client methods that return a `Response<T>>` asynchronously buffer the entire model within the internal pipeline, so `into_body()` and other methods on the response are no longer async.

