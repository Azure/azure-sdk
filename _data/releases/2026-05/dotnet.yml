entries:
- Name: Azure.ResourceManager.ContainerServiceFleet
  Version: 1.2.0-beta.3
  DisplayName: Resource Management - Container Service Fleet
  ServiceName: Container Service Fleet
  VersionType: Beta
  Hidden: false
  ChangelogUrl: https://github.com/Azure/azure-sdk-for-net/tree/Azure.ResourceManager.ContainerServiceFleet_1.2.0-beta.3/sdk/fleet/Azure.ResourceManager.ContainerServiceFleet/CHANGELOG.md
  ChangelogContent: ""
- Name: Azure.Security.KeyVault.Secrets
  Version: 4.10.0
  DisplayName: Key Vault - Secrets
  ServiceName: Key Vault
  VersionType: GA
  Hidden: false
  ChangelogUrl: https://github.com/Azure/azure-sdk-for-net/tree/Azure.Security.KeyVault.Secrets_4.10.0/sdk/keyvault/Azure.Security.KeyVault.Secrets/CHANGELOG.md
  ChangelogContent: |-
    #### Features Added

    - Added `AddSecretClient` and `AddKeyedSecretClient` extension methods on `IHostApplicationBuilder` for registering `SecretClient` via dependency injection using configuration-based setup.
    - Added a JSON schema segment to the NuGet package that provides IntelliSense and validation for `SecretClientSettings` configuration in `appsettings.json`.
- Name: Azure.Identity.Broker
  Version: 1.6.0
  DisplayName: Identity Broker
  ServiceName: Other
  VersionType: GA
  Hidden: false
  ChangelogUrl: https://github.com/Azure/azure-sdk-for-net/tree/Azure.Identity.Broker_1.6.0/sdk/identity/Azure.Identity.Broker/CHANGELOG.md
  ChangelogContent: |-
    #### Other Changes

    - Updated dependency from `Azure.Identity` to `Azure.Core` directly, as the `Azure.Identity` types moved to `Azure.Core`.
    - **This version is required when using `Azure.Identity` 1.21.0 or later (or `Azure.Core` 1.53.0 or later).** Older `Azure.Identity.Broker` versions are not compatible with the type consolidation. See the [migration guide](https://github.com/Azure/azure-sdk-for-net/blob/main/sdk/identity/Azure.Identity/MigrationGuide.md#azureidentitybroker-compatibility) for details.
- Name: Azure.AI.AgentServer.Core
  Version: 1.0.0-beta.21
  DisplayName: AI Agent Server - Core
  ServiceName: Cognitive Services
  VersionType: Beta
  Hidden: false
  ChangelogUrl: https://github.com/Azure/azure-sdk-for-net/tree/Azure.AI.AgentServer.Core_1.0.0-beta.21/sdk/agentserver/Azure.AI.AgentServer.Core/CHANGELOG.md
  ChangelogContent: |-
    #### Breaking Changes

    - **Package split**: All Responses API protocol types (models, invocation handlers, SSE streaming) have moved to `Azure.AI.AgentServer.Responses`. All Invocations protocol types have moved to `Azure.AI.AgentServer.Invocations`. This package now contains only the shared hosting foundation.
    - **Dependency removed**: `Azure.AI.AgentServer.Contracts` is no longer required. The generated OpenAI Responses API models are now built into `Azure.AI.AgentServer.Responses`.
    - **Dependencies removed**: `Azure.AI.Projects`, `Microsoft.Agents.AI.*`, and `ModelContextProtocol` packages are no longer dependencies of this package.
    - **API redesigned**: The old `IAgentInvocation` / `AgentInvocationContext` / `CreateResponseRequest` API surface has been replaced with `AgentHostBuilder` and protocol-specific handler abstractions (`ResponseHandler` in Responses, `InvocationHandler` in Invocations).
    - **Namespace changed**: Code that previously used `Azure.AI.AgentServer.Core.Responses.*` or `Azure.AI.AgentServer.Contracts.*` namespaces must switch to `Azure.AI.AgentServer.Responses`.

    #### Features Added

    - Library-owned hosting foundation via `AgentHostBuilder` (composable builder pattern).
    - OpenTelemetry integration with `Azure.Monitor.OpenTelemetry.AspNetCore` and OTLP exporter support.
    - Health endpoint at `/readiness` for liveness and readiness probes.
    - Multi-protocol composition via `AgentHostBuilder.RegisterProtocol()`. Protocol packages provide extension methods (e.g., `AddResponses<T>()`, `AddInvocations<T>()`) built on top of this API.
    - Graceful shutdown with configurable drain period.
    - Server user-agent `x-platform-server` header on every response with SDK version info.
    - `ServerUserAgentRegistry` for protocol packages to register user-agent identity segments.
    - `AddAgentServerUserAgent()` and `UseAgentServerUserAgent()` extensions for standalone (Tier 3) setups.
    - `FoundryEnvironment` for Azure AI Foundry platform variable resolution.
    - Distributed tracing context propagation via request ID baggage.

    ### Previous versions (prior to 1.0.0-beta.21)

    Versions prior to `1.0.0-beta.21` used a monolithic architecture where `Azure.AI.AgentServer.Core`
    bundled protocol logic and depended on `Azure.AI.AgentServer.Contracts` for generated models.
    These versions are superseded by the new 3-package architecture. See the
    [Migration Guide](https://github.com/Azure/azure-sdk-for-net/blob/main/sdk/agentserver/Azure.AI.AgentServer.Core/MigrationGuide.md)
    for details.
- Name: Azure.Identity
  Version: 1.17.2
  DisplayName: Identity
  ServiceName: Other
  VersionType: Patch
  Hidden: false
  ChangelogUrl: https://github.com/Azure/azure-sdk-for-net/tree/Azure.Identity_1.17.2/sdk/identity/Azure.Identity/CHANGELOG.md
  ChangelogContent: |-
    #### Other Changes

    - Updated `Microsoft.Identity.Client` and `Microsoft.Identity.Client.Extensions.Msal` dependencies to version 4.83.1.
- Name: Azure.AI.AgentServer.Invocations
  Version: 1.0.0-beta.1
  DisplayName: unknown
  ServiceName: unknown
  VersionType: Beta
  Hidden: false
  ChangelogUrl: https://github.com/Azure/azure-sdk-for-net/tree/Azure.AI.AgentServer.Invocations_1.0.0-beta.1/sdk/agentserver/Azure.AI.AgentServer.Invocations/CHANGELOG.md
  ChangelogContent: |-
    #### Features Added

    - Initial release of the Azure.AI.AgentServer.Invocations library.
    - `InvocationHandler` abstract class for implementing invocation protocol endpoints.
    - `InvocationContext` providing request metadata and session information to handlers.
    - Automatic session ID resolution from query parameters and environment variables for multi-turn invocation tracking.
    - Automatic forwarding of `x-client-*` request headers to handlers.
    - Invocation lifecycle operations: `HandleAsync`, `GetAsync`, `CancelAsync`, `GetOpenApiAsync`.
    - `InvocationsActivitySource` for OpenTelemetry distributed tracing integration.
    - ASP.NET Core hosting integration via `AddInvocations<THandler>()` builder extension.
    - Protocol identity registration with `ServerUserAgentRegistry` during route mapping.
- Name: Azure.AI.AgentServer.Responses
  Version: 1.0.0-beta.1
  DisplayName: unknown
  ServiceName: unknown
  VersionType: Beta
  Hidden: false
  ChangelogUrl: https://github.com/Azure/azure-sdk-for-net/tree/Azure.AI.AgentServer.Responses_1.0.0-beta.1/sdk/agentserver/Azure.AI.AgentServer.Responses/CHANGELOG.md
  ChangelogContent: |-
    #### Features Added

    - Initial release of the Azure.AI.AgentServer.Responses library.
    - ASP.NET Core server library implementing the Azure AI Responses API.
    - `ResponseHandler` abstract class for custom response handling with `CreateAsync` returning `IAsyncEnumerable<ResponseStreamEvent>`.
    - `TextResponse` convenience class for simple text-only responses with a single delegate.
    - `ResponseEventStream` builder for full control over SSE event generation with automatic `sequenceNumber`, `outputIndex`, `contentIndex`, and `itemId` tracking.
    - `ResponseEventStream` convenience generators for emitting complete output items in one call:
    - `OutputItemMessage(string text)` — emits a full text message output item (handles all inner SSE events automatically)
    - `OutputItemMessage(string text, IEnumerable<Annotation> annotations)` — emits a text message with file annotations
    - `OutputItemMessage(IAsyncEnumerable<string> tokens, CancellationToken)` — streams tokens as `response.output_text.delta` events
    - `OutputItemFunctionCall(name, callId, arguments)` — emits a complete function call output item
    - `OutputItemFunctionCallOutput(callId, output)` — emits a function call output (no deltas)
    - `OutputItemReasoningItem(...)` — emits a reasoning output item
    - `OutputItemImageGenCall(resultBase64)` — emits an image generation result with status transitions
    - `OutputItemStructuredOutputs(output)` — emits an arbitrary structured JSON output item
    - One-liner convenience generators for all remaining simple output item types: computer calls, local shell calls, function shell calls, apply-patch calls, custom tool call outputs, MCP approval requests/responses, and compaction
    - `ResponseContext` with `GetInputItemsAsync()` (returns `IReadOnlyList<Item>`) and `GetInputTextAsync()` convenience for accessing resolved input items and text content.
    - `IEnumerable<Item>.GetInputText()` extension method for extracting text from any sequence of input items.
    - `CreateResponse.GetInputExpanded()` extension for advanced access to expanded input items as `OutputItem` types.
    - Built-in in-memory response provider and execution tracking.
    - Support for default, streaming, background, and streaming+background response modes.
    - `AgentHostBuilder` convenience methods for zero-config server startup via `ResponsesServer.Run<T>()`.
    - Protocol identity registration with `ServerUserAgentRegistry` during route mapping.
    - `x-agent-response-id` header validation matching the Responses API specification.
    - Conversation ID round-trip support in both synchronous and SSE streaming modes.
    - OpenTelemetry distributed tracing via `Azure.AI.AgentServer.Responses` activity source.

